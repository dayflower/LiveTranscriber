<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- Not sandboxed: TCC prompts (Microphone / Screen Recording / Calendars)
	     are driven by Info.plist usage descriptions and runtime API calls, so
	     no sandbox entitlements are needed. Kept minimal as a hook for future
	     needs (e.g. get-task-allow for attaching a debugger). -->
</dict>
</plist>
